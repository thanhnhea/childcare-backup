import { Button } from 'react-bootstrap';
import './CreatePostButton.css'

const CreatePostButton = () => {
    return (
        <Button variant="primary" className="create-post-button">
            Create Post
        </Button>
    );
};

export default CreatePostButton;

// CSS styles
